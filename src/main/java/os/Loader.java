package main.java.os;

public class Loader {
    public Process load(String exeName) {
        // read file
        // parse header
        Process process = new Process();
//        process.initialize(exeName);
        return new Process();
    }
}
